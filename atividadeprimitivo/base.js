let base = 4;
let altura = 2;
 
let area = (base * altura) / 2
 
console.log (`A área de um triangulo é base vezes altura dividido por 2 que utilizando os valores ${base} para base e ${altura} para a altura é igual a ${area}`)